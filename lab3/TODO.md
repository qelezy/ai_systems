# TODO

## 1. Поправить график, чтобы отражал уровни истинности предпосылок
- Реализовать вычисление firing levels (αᵢ) для каждого правила.
- Добавить метод `plot_firing_levels` в `FuzzyPlotWidget`.
- Обеспечить построение зависимостей αᵢ от входных переменных.

## 2. В текстовом поле результатов отразить вычисленные уровни истинности предпосылок
- Добавить вывод firing levels для всех правил в методах:
  - `calculate_single`
  - `calculate_multiple`
- Форматированный вывод:
  - Правило №: α = ...

## 3. Исправить функционал сравнения импликации
- Проверить корректность применения типов импликации (Мин, Прод, Ларсен и др.).
- Исправить сравнение так, чтобы:
  - для каждого типа импликации вычислялся отдельный график
  - графики отображались корректно в `plot_comparison`
- Убедиться, что все типы импликации корректно передаются в движок нечёткого вывода.

## 4. Сделать 2 часть работы (с нелинейной функцией)
- Добавить пункт в интерфейсе для выбора «Часть 2 — нелинейная функция».
- Добавить нелинейную функцию.
- Реализовать вычисление результатов с помощью модели Мамдани и модели Такаги-Сугено.
- Сделать сравнение результатов моделей Мамдани и Такаги-Сугено с исходной функцией.
- Реализовать построетние поверхности отображения Мамдани и Такаги-Сугено.